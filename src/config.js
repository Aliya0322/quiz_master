const DEFAULT_PLUGINS = ['history','science'];
const MAX_QUESTIONS_PER_ROUND = 10;

const config = {
    themes: DEFAULT_PLUGINS,
    maxQuestions: MAX_QUESTIONS_PER_ROUND
}

export default config;
export {DEFAULT_PLUGINS, MAX_QUESTIONS_PER_ROUND}